<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css" />


<div class="min-h-screen flex items-center justify-center px-4">

  <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">

    <!-- Title -->
    <h1 class="text-2xl font-semibold text-center mb-2">Verify OTP</h1>

    <p class="text-gray-600 text-center mb-6">
      Please enter the 6-digit verification code sent to your email.
    </p>

    <!-- OTP FORM -->
    <form action="/user/verify-otp" method="POST" id="otpForm">

      <div class="flex justify-between mb-6">
        <% for(let i=0; i<6; i++) { %>
          <input type="text" maxlength="1" class="otp-input w-12 h-14 text-center border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-600 focus:border-blue-600 text-lg" name="otp" />
          <% } %>
      </div>

      <button type="submit" class="w-full bg-blue-900 hover:bg-blue-950 text-white font-semibold py-3 rounded-lg">
        Verify OTP
      </button>
    </form>

    <!-- Timer -->
    <p class="text-center text-gray-600 text-sm mt-4">
      OTP will expire in <span id="otp-timer">02:00</span>
    </p>

    <!-- Resend OTP -->
    <p class="text-center text-sm mt-4">
      Didn't receive the code?
      <a id="resendBtn" class="text-blue-700 font-semibold cursor-pointer">Resend OTP</a>
    </p>

  </div>

</div>

<script src="/js/user/verifyOtp.js"></script>